# PostgreSQL connection for my-rent-finder
PGHOST=127.0.0.1
PGHOST_PORT=5432
PGPORT=5432
PGDATABASE=my_rent_finder
PGUSER=postgres
PGPASSWORD=postgres
PGADMIN_USER=postgres
PGADMIN_PASSWORD=postgres

# PGHOST_PORT == 로컬 호스트에서 PostgreSQL 포트 매핑 값
# (기본값: 5432, 충돌 시 15432 등으로 바꾸면 PGHOST/PGPORT도 동일한 값으로 바꿔주세요)

# Optional: 운영자 계정 분리 시 사용
# PGUSER/PGPASSWORD = 앱(DB 읽기/쓰기) 계정
# PGADMIN_USER/PGADMIN_PASSWORD = 부트스트랩/초기화 계정

# Optional: can be set instead of PG* values

# Optional: prefer one-line URL instead of PG* vars
# DATABASE_URL=postgres://postgres:your_password_here@127.0.0.1:5432/my_rent_finder
