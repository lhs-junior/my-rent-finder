# ============================================================
# my-rent-finder 환경변수 설정
# ============================================================
# 이 파일을 .env 로 복사한 뒤 값을 수정하세요.
#   cp .env.example .env

# ------------------------------------------------------------
# PostgreSQL 접속 정보 (필수)
# ------------------------------------------------------------
PGHOST=127.0.0.1
PGPORT=5432
PGDATABASE=my_rent_finder
PGUSER=postgres
PGPASSWORD=postgres

# PGHOST_PORT: 로컬 호스트에서 PostgreSQL 포트 매핑 값
# (기본값: 5432, 충돌 시 15432 등으로 바꾸면 PGHOST/PGPORT도 동일한 값으로 바꿔주세요)
PGHOST_PORT=5432

# ------------------------------------------------------------
# PostgreSQL 관리자 계정 (docker-compose 초기화용)
# ------------------------------------------------------------
PGADMIN_USER=postgres
PGADMIN_PASSWORD=postgres

# ------------------------------------------------------------
# 대체 접속 방식: DATABASE_URL (PG* 변수 대신 사용 가능)
# ------------------------------------------------------------
# DATABASE_URL=postgres://postgres:postgres@127.0.0.1:5432/my_rent_finder

# ------------------------------------------------------------
# API 서버 설정 (선택)
# ------------------------------------------------------------
# API_HOST=127.0.0.1
# API_PORT=4100
